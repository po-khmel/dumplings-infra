apiVersion: v1
kind: Service
metadata:
  name: {{ include "dumplings-helm-charts.fullname" . }}
  labels:
    {{- include "dumplings-helm-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dumplings-helm-charts.selectorLabels" . | nindent 4 }}
